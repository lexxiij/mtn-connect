// environment.ts — used during LOCAL DEVELOPMENT (ng serve)
//
// When you run "ng serve", Angular uses THIS file.
// Your backend runs on localhost:3000, so the API URL points there.

export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
};
